export enum PhraseDateOperator {
  GT = '>=',
  LT = '<=',
}

export enum PhraseWideSort {
  NAME = 'name',
  KEY_NAME = 'key_name',
  UPDATED_AT = 'updated_at',
  CREATED_AT = 'created_at',
}

export enum PhraseOrder {
  ASC = 'asc',
  DESC = 'desc',
}
